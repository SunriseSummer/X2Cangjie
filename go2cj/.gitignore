# Test-generated artifacts (regenerated by tests/run_tests.py)
tests/generated/

# Python bytecode
__pycache__/
*.pyc

# Training "last" checkpoint — used to resume training across sessions.
# Only the best-so-far model.pt is shipped.
go2cj/model_last.pt

