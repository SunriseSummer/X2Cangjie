let a: Int64 = 7

let b: Int64 = 3

main() {
    println("${a} + ${b} = ${a + b}")
    println("${a} - ${b} = ${a - b}")
    println("${a} * ${b} = ${a * b}")
    println("${a} / ${b} = ${a / b}")
    println("${a} % ${b} = ${a % b}")
    return 0
}
