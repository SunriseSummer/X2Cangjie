// Demonstrates a couple of edge cases the converter handles best-effort.
const PI: number = 3;            // integer approximation of pi
const r: number = 10;
const area: number = PI * r * r;
console.log(`area approx ${area}`);
