main() {
    let a = 12
    let b = 25
    let maxv = if (a > b) {
        a
    } else {
        b
    }
    let minv = if (a < b) {
        a
    } else {
        b
    }
    println("max=${maxv} min=${minv}")
}
